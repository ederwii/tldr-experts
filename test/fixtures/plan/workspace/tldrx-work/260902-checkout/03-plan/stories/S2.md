---
version: 1
id: S2
epic: E1
title: "POST /orders accepts a guest"
repo: api
status: done
depends_on: []
touches: ["src/Api/Orders/"]
acceptance: ["a request with no auth header creates an order"]
test_plan: ["integration test over the endpoint"]
evidence: ["commit 9c30b71 — guest branch on the orders controller"]
---

# S2 — POST /orders accepts a guest

Reopened once: the first cut let an authenticated caller through the guest branch as well, which is a different order shape. See the ledger.

```dod
dotnet test
```
