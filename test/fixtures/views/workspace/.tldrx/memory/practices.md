# Practices

How this team works, learned from runs. Appended by `tldrx retro --apply`;
every bullet cites the events line that produced it.

## 2026-08-14 — run 260814-envs

- Regenerate the SDK in the same commit as the DTO change [src: tldrx-work/260814-envs/events.jsonl:31]
