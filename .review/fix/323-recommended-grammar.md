verdict: merge
reviewed-by: pre-merge reviewer subagent (claude-sonnet-5), separate from the implementer (claude-opus-5); the reviewer's em-dash compatibility line was an instrument error, re-measured by the driver with bun: em-dash, `A)` and hyphen reasons are readable under both the old regex and the new parser
against: 1e708bf
