{
  "name": "tldr-experts",
  "version": "0.0.1",
  "description": "A lightweight, file-based AI development workflow framework. Pre-alpha.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "tldrx": "dist/tldrx.js"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "doctor": "bun bin/tldrx.ts doctor"
  },
  "files": [
    "dist",
    "stages",
    "workflows",
    "templates",
    "plugin",
    "env.yml"
  ],
  "engines": {
    "node": ">=20",
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2",
    "yaml": "^2.9.0"
  }
}
