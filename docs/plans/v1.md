# v1 / v1.1 plan (started 2026-08-29)

Continuation instructions for any session: each wave = Opus agents in git worktrees
(`~/tldr-experts-wt/<name>`, branch `waveN/<name>`), draft PR for Linux CI, then
`scripts/merge-wave.sh <branch> "<msg>"` from main. Release = bump version + CHANGELOG +
`git tag vX.Y.Z && git push origin vX.Y.Z` (trusted publishing). Pilot workspace:
/Users/alanmartinez/scavtopia (run 260829-scoring-leaderboard, parked at the What gate).

| Wave | Scope | Release |
|---|---|---|
| 4A | story/epic/waves schemas + templates; `tldrx budget show|raise` (retry not blocked by its own first attempt; per-attempt accounting in status); checked sections must contain list items | 0.1.0 |
| 4B | greenfield: `init` asks intended stack when none detected and seeds `product` + stack experts; `run new --seed <file|dir>` generic document import (md/txt) → declared What inputs + distilled Findings `[src: path:line]` | 0.1.0 |
| 5 | Build phase: waves.yml → worktree + branch per story, epic branches, DoD gate = workspace commands, reviewer pass; pilot: one leaderboard story | 0.2.0 |
| 6 | Watch phase (`watchers/*.md` from Build's instrumentation) + live dashboard (watcher + SSE, read-only). Alan 2026-08-29: keep it simple yet useful — one JSON model from the files, thin templating, no framework — a UI/UX redesign will replace the rendering layer later, so keep data model and renderer separate | 0.3.0 |
| 7 | Expert training light/full with provenance → competencies evidence → star chart | 0.4.0 |
| 8A | `tldrx install --claude` (skill + hooks + statusline into .claude/, idempotent, --uninstall), `tldrx hook <name>` / `tldrx statusline`, `tldrx interview` (terminal Q&A over open questions) — Alan 2026-08-29: the facilitator skill must be installable independently of `init` | 0.3.0/0.4.0 |
| 8B | Adapters: ticket mirror (Jira/GitHub, mirror-out + external_status), chat channel | 0.5.0 |

Status log (append one line per merged wave): 
- 2026-08-29 wave4/greenfield merged (17b1a55, 496 tests): init --stack + product/stack experts + mode greenfield; run new --seed <file|dir>.
- 2026-08-29 wave4/schemas merged (b1b3888, 560 tests): story/epic/waves schemas, tldrx budget show|raise, sections must hold items. Released 0.1.0.
- 2026-08-29 wave6/dashboard merged: live dashboard (model.json + SSE), model/renderer split for the later UI/UX pass.
- 2026-08-29 wave6/watch merged: watcher cards (05-watch/watchers/*.md, draft|verified), watch executor, tldrx watch list|check; executors registry created here.
- 2026-08-29 wave6/watch merged: watcher cards, watch executor, tldrx watch list|check; executors registry created here.
- 2026-08-29 wave5/build merged (3add923, 634 tests): Build executor; executorFor wired. Released 0.2.0 = Build + Watch + live dashboard.
